import * as React from "react";
import { Box } from "rebass";

const Page = (props) => {
  const [value, setValue] = React.useState("");

  return <Box width={256}>123</Box>;
};

// Page.getInitialProps = async (ctx) => {
//   return {};
// };

export default Page;
